# Copy this file to .env and set values for your environment
# Set `DB_HOST` to your private subnet DB endpoint (required; do NOT use 'localhost' for production)
DB_HOST=
DB_USER=root
DB_PASSWORD=
DB_NAME=hospital
DB_PORT=3306

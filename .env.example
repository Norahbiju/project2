### Copy this file to .env and fill values for your private subnet deployment
# Do NOT use 'localhost' here; use the host reachable from your app in the private subnet
DB_HOST=10.0.0.5
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=hospital

# Example: DB_HOST=10.0.1.7